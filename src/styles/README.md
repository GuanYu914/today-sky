# Styles

Global styles and shared CSS modules.

## Files

- `variables.css` - CSS custom properties (colors, spacing, etc.)
- `sky-gradients.css` - Sky color gradients for different times of day
- `animations.css` - Reusable animations
