# 測試覆蓋率指南

## 查看測試覆蓋率

### 命令列報告

```bash
npm run test:coverage
```

這會在終端機中顯示覆蓋率摘要表格。

### HTML 詳細報告

```bash
npm run test:coverage
```

然後在瀏覽器中開啟 `coverage/index.html` 查看詳細的覆蓋率報告，包括：

- 每個檔案的行覆蓋率
- 分支覆蓋率
- 函式覆蓋率
- 未覆蓋的程式碼行高亮顯示

### 覆蓋率門檻

專案設定了 80% 的最低覆蓋率門檻：

- 行覆蓋率 (Lines): 80%
- 函式覆蓋率 (Functions): 80%
- 分支覆蓋率 (Branches): 80%
- 陳述句覆蓋率 (Statements): 80%

如果覆蓋率低於門檻，測試會失敗。

## 當前狀態

目前專案的覆蓋率為 **100%**，已超過目標。

## 排除的檔案

以下檔案類型不計入覆蓋率：

- 測試檔案 (`*.test.ts`, `*.spec.ts`)
- 配置檔案 (`*.config.ts`)
- 型別定義檔案 (`*.d.ts`)
- 進入點檔案 (`main.tsx`)
- 端對端測試檔案 (`e2e/`)

## CI/CD 整合

在 CI/CD 流程中，可以使用以下指令確保測試覆蓋率符合標準：

```bash
npm run test:coverage
```

覆蓋率報告會輸出為多種格式：

- `text` - 終端機顯示
- `json` - 機器可讀格式
- `html` - 詳細的互動式報告
- `lcov` - 用於 CI/CD 工具整合
